# This file is autogenerated by maturin v1.7.1
# To update, run
#
#    maturin generate-ci github -m ./music-mesa-tables-py/Cargo.toml --platform linux macos
#
name: CI

on:
  push:
    branches:
      - master
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@1.88
      - run: cargo test

  build:
    runs-on: ${{ matrix.platform.os }}
    permissions:
      contents: write
    needs: [tests]
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
          - os: windows-latest
            target: x86_64-pc-windows-gnu
    steps:
      - name: install `musl-tools`
        if: runner.os == 'Linux'
        run: sudo apt install -y musl-tools
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@1.88
        with:
          targets: ${{ matrix.platform.target }}
      - name: build
        shell: bash
        run: |
          cargo build --release --target ${{ matrix.platform.target }}
          binname="pipeline"
          asset_name=$binname-${{ matrix.platform.target }}
          if [ "${{ matrix.platform.os }}" = "windows-latest" ]; then
            target="target/${{ matrix.platform.target }}/release/$binname.exe"
          else
            target="target/${{ matrix.platform.target }}/release/$binname"
          fi
          echo "ASSET=$target" >> $GITHUB_ENV
          echo "ASSET_NAME=$asset_name" >> $GITHUB_ENV
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ASSET_NAME }}
          path: ${{ env.ASSET }}
      - uses: softprops/action-gh-release@v2
        if: github.ref_type == 'tag'
        with:
          files: ${{ env.ASSET }}
