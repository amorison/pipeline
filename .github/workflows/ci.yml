# This file is autogenerated by maturin v1.7.1
# To update, run
#
#    maturin generate-ci github -m ./music-mesa-tables-py/Cargo.toml --platform linux macos
#
name: CI

on:
  push:
    branches:
      - master
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@1.88
      - run: cargo test

  build:
    runs-on: ${{ matrix.platform.os }}
    needs: [tests]
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: windows-latest
            target: x86_64-pc-windows-gnu
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@1.88
        with:
          targets: ${{ matrix.platform.target }}
      - run: cargo build --release --target ${{ matrix.platform.target }}
      - name: build archive
        shell: bash
        run: |
          binname="pipeline"
          dirname="$binname-${{ matrix.platform.target }}"
          mkdir "$dirname"
          if [ "${{ matrix.platform.os }}" = "windows-latest" ]; then
            mv "target/${{ matrix.platform.target }}/release/$binname.exe" "$dirname"
          else
            mv "target/${{ matrix.platform.target }}/release/$binname" "$dirname"
          fi
          7z a "$dirname.zip" "$dirname"
          echo "ASSET=$dirname.zip" >> $GITHUB_ENV
      - uses: actions/upload-artifact@v4
        with:
          name: pipeline-${{ matrix.platform.target }}
          path: ${{ env.ASSET }}
